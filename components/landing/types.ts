export interface Provider {
  name: string;
  description: string;
  accent: string;
  logo: string;
}

export interface Agent {
  name: string;
  description: string;
  accent: string;
  logo: string;
}
